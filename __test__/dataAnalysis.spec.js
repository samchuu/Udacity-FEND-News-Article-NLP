import { analyzeData } from "../src/client/js/dataAnalysis"

describe("Testing the submit functionality", () => {
  
    test("Testing the analyzeData() function", () => {
           expect(analyzeData).toBeDefined();
})});